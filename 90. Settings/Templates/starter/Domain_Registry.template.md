# Domain Registry — 도메인 좌표계 마스터

> 위키 전체의 `domains` frontmatter 값의 유일한 진실 원천 — 사용자가 일을 나누는 실제 축.
> **확장 절차**: 새 역할·팀·영역 = 이 표에 1줄 추가 + `WS-Leaf.template.md`로 `20. Wiki/25. Maps/WS — {Domain}.md` 1장 생성 + 허브 맵에 링크. 기존 페이지 이동 없음.
> 값 추가·개명은 사용자 합의 후 이 파일과 CLAUDE.md 버전 기록에 남긴다.

## {{company_branch_name}}

| 값 | 좌표 (역할·영역) | WS 맵 | 설명 |
|---|---|---|---|
{{domain_table_company}}

## {{personal_branch_name}}

| 값 | 좌표 (역할·영역) | WS 맵 | 설명 |
|---|---|---|---|
{{domain_table_personal}}

## 부여 판정 규칙 (다중 허용)

- "이 지식을 재사용할 때 나는 어느 모자(역할)를 쓰고 있는가"로 판정 (1차 렌즈: {{primary_lens}})
- 두 좌표 이상에 실질 재사용처가 있으면 다중 부여. 억지로 하나로 좁히지 않는다.
- 판단이 안 서면 사용자에게 묻는다 (Gold In Gold Out과 동일 원칙).

---

| 버전 | 일자 | 내용 |
|---|---|---|
| v1 | {{today}} | 설치 인터뷰(좌표 단계)로 최초 작성 |
