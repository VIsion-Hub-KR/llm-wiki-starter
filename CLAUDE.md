# LLM Wiki 스타터 킷 — 미설치 상태

> 이 폴더는 **아직 설치되지 않은** LLM Wiki 스타터 킷이다.

## 이 상태에서의 규칙 (AI 필독)

1. 사용자가 무엇을 요청하든, 먼저 **`/setup` 실행을 안내**한다 — "아직 설치 전입니다. `/setup`을 입력하시면 15~20분 인터뷰로 맞춤 세팅을 시작합니다."
2. `/setup` 인터뷰가 완료되기 전에는 **어떤 위키 파일도 만들지 않는다** (Raw·Wiki 페이지·index 등 전부).
3. 설치가 완료되면 이 문서는 `/setup`이 생성한 본편 CLAUDE.md로 **교체**된다 — 이 문서가 보인다면 아직 미설치 상태다.
4. 사용법·철학이 궁금하면 `README.md`를 안내한다.
